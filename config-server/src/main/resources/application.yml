server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: file:///D:/Eclipse Workspace/ecomm/ecom-microservices/config-repo
          default-label: master
          clone-on-start: true
